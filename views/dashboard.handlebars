{{#if posts.length}}
<div>
  <h3 class="d-flex justify-content-center">Your Blog Posts</h3>
  {{#each posts as |post|}}
    {{> dashboard-details}}
  {{/each}}
  {{else}}
  <h3>You have not made a blog post yet.</h3>
</div>
{{/if}}